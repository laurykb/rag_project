# core/ — Logique métier de la pipeline RAG
# ask.py       : orchestrateur (query → rewrite → retrieve → réponse)
# ingest.py    : ingestion (markdown → chunks → embeddings → index)
# llm_answer.py: construction du contexte + appel LLM
